== scrot截图 ==

`scort <文件名>`默认产生的文件名是时间加上图像大小。

常用参数：

* `-bs`：抓取窗口
* `-s`：抓矩形
* `-cd <延迟的秒数>`：其中d表示延时抓取，c显示倒计时。
* `-t <百分比>`：缩略图。
* `-q <1-100>`：图像压缩等级，默认是75
* `-e <操作>`：截图后执行的操作。如移动到指定目录：`-e 'mv $f ~/tmp/'`
